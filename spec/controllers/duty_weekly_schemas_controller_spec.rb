require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe DutyWeeklySchemasController do

  # This should return the minimal set of attributes required to create a valid
  # DutyWeeklySchema. As you add validations to DutyWeeklySchema, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "sunday" => "" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # DutyWeeklySchemasController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all duty_weekly_schemas as @duty_weekly_schemas" do
      duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
      get :index, {}, valid_session
      assigns(:duty_weekly_schemas).should eq([duty_weekly_schema])
    end
  end

  describe "GET show" do
    it "assigns the requested duty_weekly_schema as @duty_weekly_schema" do
      duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
      get :show, {:id => duty_weekly_schema.to_param}, valid_session
      assigns(:duty_weekly_schema).should eq(duty_weekly_schema)
    end
  end

  describe "GET new" do
    it "assigns a new duty_weekly_schema as @duty_weekly_schema" do
      get :new, {}, valid_session
      assigns(:duty_weekly_schema).should be_a_new(DutyWeeklySchema)
    end
  end

  describe "GET edit" do
    it "assigns the requested duty_weekly_schema as @duty_weekly_schema" do
      duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
      get :edit, {:id => duty_weekly_schema.to_param}, valid_session
      assigns(:duty_weekly_schema).should eq(duty_weekly_schema)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new DutyWeeklySchema" do
        expect {
          post :create, {:duty_weekly_schema => valid_attributes}, valid_session
        }.to change(DutyWeeklySchema, :count).by(1)
      end

      it "assigns a newly created duty_weekly_schema as @duty_weekly_schema" do
        post :create, {:duty_weekly_schema => valid_attributes}, valid_session
        assigns(:duty_weekly_schema).should be_a(DutyWeeklySchema)
        assigns(:duty_weekly_schema).should be_persisted
      end

      it "redirects to the created duty_weekly_schema" do
        post :create, {:duty_weekly_schema => valid_attributes}, valid_session
        response.should redirect_to(DutyWeeklySchema.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved duty_weekly_schema as @duty_weekly_schema" do
        # Trigger the behavior that occurs when invalid params are submitted
        DutyWeeklySchema.any_instance.stub(:save).and_return(false)
        post :create, {:duty_weekly_schema => { "sunday" => "invalid value" }}, valid_session
        assigns(:duty_weekly_schema).should be_a_new(DutyWeeklySchema)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        DutyWeeklySchema.any_instance.stub(:save).and_return(false)
        post :create, {:duty_weekly_schema => { "sunday" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested duty_weekly_schema" do
        duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
        # Assuming there are no other duty_weekly_schemas in the database, this
        # specifies that the DutyWeeklySchema created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        DutyWeeklySchema.any_instance.should_receive(:update).with({ "sunday" => "" })
        put :update, {:id => duty_weekly_schema.to_param, :duty_weekly_schema => { "sunday" => "" }}, valid_session
      end

      it "assigns the requested duty_weekly_schema as @duty_weekly_schema" do
        duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
        put :update, {:id => duty_weekly_schema.to_param, :duty_weekly_schema => valid_attributes}, valid_session
        assigns(:duty_weekly_schema).should eq(duty_weekly_schema)
      end

      it "redirects to the duty_weekly_schema" do
        duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
        put :update, {:id => duty_weekly_schema.to_param, :duty_weekly_schema => valid_attributes}, valid_session
        response.should redirect_to(duty_weekly_schema)
      end
    end

    describe "with invalid params" do
      it "assigns the duty_weekly_schema as @duty_weekly_schema" do
        duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        DutyWeeklySchema.any_instance.stub(:save).and_return(false)
        put :update, {:id => duty_weekly_schema.to_param, :duty_weekly_schema => { "sunday" => "invalid value" }}, valid_session
        assigns(:duty_weekly_schema).should eq(duty_weekly_schema)
      end

      it "re-renders the 'edit' template" do
        duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        DutyWeeklySchema.any_instance.stub(:save).and_return(false)
        put :update, {:id => duty_weekly_schema.to_param, :duty_weekly_schema => { "sunday" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested duty_weekly_schema" do
      duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
      expect {
        delete :destroy, {:id => duty_weekly_schema.to_param}, valid_session
      }.to change(DutyWeeklySchema, :count).by(-1)
    end

    it "redirects to the duty_weekly_schemas list" do
      duty_weekly_schema = DutyWeeklySchema.create! valid_attributes
      delete :destroy, {:id => duty_weekly_schema.to_param}, valid_session
      response.should redirect_to(duty_weekly_schemas_url)
    end
  end

end
